# Same as arrays

string=Hello

# to print all elements
echo The array has got elements : "${string[@]}"

# to print the length of the array(which is always 1)
echo Length of array is : "${#string[@]}"
